// with out arguments and with return value
#include <stdio.h>
#include <stdlib.h>

int getNumber(){
int num;
printf("enter a number:");
scanf("%d",&num);
return num;
}
int main(){
int n = getNumber();
printf("you entered:%d",n);
}
