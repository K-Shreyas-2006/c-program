// function returning pointer to an array element
#include <stdio.h>

int* getelements(int arr[],int index)
{
    return &arr[index];
}
int main()
{
   int arr[5] = {2, 3 , 4, 5, 5 };
   int *p= getelements (arr,2);
   printf("elements at index 2 = %d\n ", *p);
   return 0;
}
