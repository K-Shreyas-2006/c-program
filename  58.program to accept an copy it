//program to accept an copy it
#include<stdio.h>
#include<string.h>
int main()
{

    char src[100];
    char des[100];
    printf("enter string to be copied");
    scanf("%s",&src);
    strcpy(des,src);


    printf("copied string: %s",des);

    return 0;
}
