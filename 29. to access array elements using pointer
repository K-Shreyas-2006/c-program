// access array elements using pointer

#include <stdio.h>

int main()
{

    int arr[5] = {10, 20 , 30, 40 ,60};
    int *p = arr;
    printf(" array elements using pointer");
    for (int i =0;i<5;i++)
        printf("%d",*(p+1));
    return 0;
}
