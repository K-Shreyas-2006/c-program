// Write a program to validate a password which has one Uppercase,one numeric and one symbol. it must have a minimum of 8 and maximum of 15 character. Print whether the password is i)weak ii) moderate iii)strong. and check whether it is valid or invalid

#include<stdio.h>
#include<string.h>
int main()
{
    char username[30]="Siddhant Divekar";
    char password[15];
    printf("Username: Siddhant Divekar\n");
    printf("Create your Password: ");
    scanf("%s",&password);
    int c=strlen(password);
    if(c==8)
    {
        printf("Password is Weak\n");
    }
    else if(c>8 && c<11)
    {
        printf("Password is Moderate\n");
    }
    else if(c>12 && c<=15)
    {
        printf("Password is Strong\n");
    }
    else if(c>15
            )
        {
            printf("Password exceeds character limit\n");
        }

    if(strcmp(password,"SiddhantDive@07")==0)
    {
        printf("Password is valid");
    }
    else
        printf("Password is invalid");

    return 0;
}
